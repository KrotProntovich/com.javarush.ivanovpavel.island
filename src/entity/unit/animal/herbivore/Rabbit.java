package entity.unit.animal.herbivore;

import util.UnitType;

public class Rabbit extends Herbivore{

    public Rabbit(long id, UnitType type, Integer weight, boolean eaten) {
        super(id, type, weight, eaten);
    }
}
