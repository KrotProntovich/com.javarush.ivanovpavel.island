package entity.unit.animal.herbivore;

import util.UnitType;

public class Sheep extends Herbivore{
    public Sheep(long id, UnitType type, Integer weight, boolean eaten) {
        super(id, type, weight, eaten);
    }
}
