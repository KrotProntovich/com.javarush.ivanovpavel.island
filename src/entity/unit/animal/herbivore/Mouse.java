package entity.unit.animal.herbivore;

import util.UnitType;

public class Mouse extends Herbivore{
    public Mouse(long id, UnitType type, Integer weight, boolean eaten) {
        super(id, type, weight, eaten);
    }
}
