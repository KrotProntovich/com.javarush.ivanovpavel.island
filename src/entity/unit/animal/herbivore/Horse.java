package entity.unit.animal.herbivore;

import util.UnitType;

public class Horse extends Herbivore{
    public Horse(long id, UnitType type, Integer weight, boolean eaten) {
        super(id, type, weight, eaten);
    }
}
