package entity.unit.animal.herbivore;

import util.UnitType;

public class Buffalo extends Herbivore {
    public Buffalo(long id, UnitType type, Integer weight, boolean eaten) {
        super(id, type, weight, eaten);
    }
}
